kind = "web"
previewPath = "/"
title = "Article Platform"
version = "1.0.0"
id = "artifacts/article-platform"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21027

[services.development]
run = "pnpm --filter @workspace/article-platform run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/article-platform", "run", "build" ]
publicDir = "artifacts/article-platform/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21027"
BASE_PATH = "/"
